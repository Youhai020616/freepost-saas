{
  "name": "freepost-saas",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "pnpm -r build",
    "build:ci": "pnpm --filter @freepost/db prisma:generate && pnpm -r build",
    "dev": "pnpm --parallel --filter @freepost/api --filter @freepost/web dev",
    "start": "pnpm -r start",
    "lint": "pnpm -r lint"
  }
}
